---
- name: Setting up ODL 2.7.2 on Docker Images via SSH
  hosts: node1 # see hosts.i:w
  become: true
  become_user: root
  become_method: sudo  

  tasks:
  - name: Test the connection
    command: "ls -al"
